export interface ReservationInfo {
    studentName: string
    selectedRoom: string
    studentId: string
    email: string
    date: string
    startTime: string
    endTime: string
    reason: string
}